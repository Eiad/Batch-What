{
  "name": "batchwhat",
  "version": "1.0.0",
  "homepage": "https://batchwhat.vercel.app/",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "next dev -p 3030",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "npm run build && npm run export && gh-pages -d out"
  },
  "dependencies": {
    "@vercel/analytics": "^1.5.0",
    "next": "14.2.35",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
